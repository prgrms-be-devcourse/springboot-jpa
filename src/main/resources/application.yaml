spring:
  datasource:
    url: jdbc:mysql://localhost:3300/test
    username: root
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
---
spring:
  datasource:
    url: jdbc:mysql://localhost:3300/test_code
    username: root
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
  config:
    activate:
      on-profile: test