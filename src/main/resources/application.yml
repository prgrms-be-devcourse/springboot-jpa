spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/test_mission;
    username: sa
    password:
  jpa:
    generate-ddl: true
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: create
  h2:
    console:
      enabled: true
