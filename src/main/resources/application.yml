spring:
  h2:
    console:
      enabled: true # h2 ?? ??
  jpa:
    generate-ddl: true # ddl? ??
    hibernate:
      ddl-auto: create-drop # SessionFactory? ??? ? drop ? ??, SessionFactory? ??? ? drop
    database: H2
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        query.in_clause_parameter_padding: true