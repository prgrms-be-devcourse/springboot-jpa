spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/.order
    username: sa
    password: 1234

  jpa:
    database: h2
    generate-ddl: true
#    show-sql: true
    properties:
      format_sql: true
    hibernate:
      ddl-auto: create-drop