spring:
  datasource:
    url: jdbc:h2:~/test
    username: sa
    password:

  h2:
    console:
      enabled: true

  jpa:
    generate-ddl: true
    hibernate.ddl-auto: create-drop
    show-sql: true
    open-in-view: false
    properties:
        hibernate:
        query.in_clause_parameter_padding: true
